<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ruoyi.ai.mapper.AiKnowledgeMapper">
    
    <resultMap type="AiKnowledge" id="AiKnowledgeResult">
        <result property="knowledgeId"    column="knowledge_id"    />
        <result property="knowledgeName"  column="knowledge_name"  />
        <result property="description"    column="description"     />
        <result property="userId"         column="user_id"         />
        <result property="fileCount"      column="file_count"      />
        <result property="status"         column="status"          />
        <result property="delFlag"        column="del_flag"        />
        <result property="createBy"       column="create_by"       />
        <result property="createTime"     column="create_time"     />
        <result property="updateBy"       column="update_by"       />
        <result property="updateTime"     column="update_time"     />
        <result property="remark"         column="remark"          />
    </resultMap>
    
    <sql id="selectKnowledgeVo">
        select knowledge_id, knowledge_name, description, user_id, file_count, status, del_flag, create_by, create_time, update_by, update_time, remark 
        from ai_knowledge
    </sql>
    
    <select id="selectKnowledgeById" parameterType="Long" resultMap="AiKnowledgeResult">
        <include refid="selectKnowledgeVo"/>
        where knowledge_id = #{knowledgeId} and del_flag = '0'
    </select>
    
    <select id="selectKnowledgeList" parameterType="AiKnowledge" resultMap="AiKnowledgeResult">
        <include refid="selectKnowledgeVo"/>
        <where>
            del_flag = '0'
            <if test="knowledgeName != null and knowledgeName != ''">
                AND knowledge_name like concat('%', #{knowledgeName}, '%')
            </if>
            <if test="userId != null">
                AND user_id = #{userId}
            </if>
            <if test="status != null and status != ''">
                AND status = #{status}
            </if>
        </where>
        order by create_time desc
    </select>
    
    <insert id="insertKnowledge" parameterType="AiKnowledge" useGeneratedKeys="true" keyProperty="knowledgeId">
        insert into ai_knowledge (
            <if test="knowledgeName != null and knowledgeName != ''">knowledge_name,</if>
            <if test="description != null and description != ''">description,</if>
            <if test="userId != null">user_id,</if>
            <if test="fileCount != null">file_count,</if>
            <if test="status != null and status != ''">status,</if>
            <if test="remark != null and remark != ''">remark,</if>
            <if test="createBy != null and createBy != ''">create_by,</if>
            create_time
        )values(
            <if test="knowledgeName != null and knowledgeName != ''">#{knowledgeName},</if>
            <if test="description != null and description != ''">#{description},</if>
            <if test="userId != null">#{userId},</if>
            <if test="fileCount != null">#{fileCount},</if>
            <if test="status != null and status != ''">#{status},</if>
            <if test="remark != null and remark != ''">#{remark},</if>
            <if test="createBy != null and createBy != ''">#{createBy},</if>
            sysdate()
        )
    </insert>
    
    <update id="updateKnowledge" parameterType="AiKnowledge">
        update ai_knowledge 
        <set>
            <if test="knowledgeName != null and knowledgeName != ''">knowledge_name = #{knowledgeName},</if>
            <if test="description != null">description = #{description},</if>
            <if test="fileCount != null">file_count = #{fileCount},</if>
            <if test="status != null and status != ''">status = #{status},</if>
            <if test="updateBy != null and updateBy != ''">update_by = #{updateBy},</if>
            update_time = sysdate()
        </set>
        where knowledge_id = #{knowledgeId}
    </update>
    
    <update id="deleteKnowledgeById" parameterType="Long">
        update ai_knowledge set del_flag = '2' where knowledge_id = #{knowledgeId}
    </update>
    
    <update id="deleteKnowledgeByIds" parameterType="String">
        update ai_knowledge set del_flag = '2' where knowledge_id in 
        <foreach item="knowledgeId" collection="array" open="(" separator="," close=")">
            #{knowledgeId}
        </foreach>
    </update>
    
</mapper>

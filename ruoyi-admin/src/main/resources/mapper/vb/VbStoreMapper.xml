<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ruoyi.vb.mapper.VbStoreMapper">
    
    <resultMap type="VbStore" id="VbStoreResult">
        <result property="storeId"      column="store_id"      />
        <result property="storeName"    column="store_name"    />
        <result property="description"  column="description"   />
        <result property="dimension"    column="dimension"     />
        <result property="vectorCount"  column="vector_count"  />
        <result property="status"       column="status"        />
        <result property="delFlag"      column="del_flag"      />
        <result property="createBy"     column="create_by"     />
        <result property="createTime"   column="create_time"   />
        <result property="updateBy"     column="update_by"     />
        <result property="updateTime"   column="update_time"   />
        <result property="remark"       column="remark"        />
    </resultMap>
    
    <sql id="selectStoreVo">
        select store_id, store_name, description, dimension, vector_count, status, del_flag, create_by, create_time, update_by, update_time, remark 
        from vb_store
    </sql>
    
    <select id="selectStoreById" parameterType="Long" resultMap="VbStoreResult">
        <include refid="selectStoreVo"/>
        where store_id = #{storeId} and del_flag = '0'
    </select>
    
    <select id="selectStoreList" parameterType="VbStore" resultMap="VbStoreResult">
        <include refid="selectStoreVo"/>
        <where>
            del_flag = '0'
            <if test="storeName != null and storeName != ''">
                AND store_name like concat('%', #{storeName}, '%')
            </if>
            <if test="status != null and status != ''">
                AND status = #{status}
            </if>
        </where>
        order by create_time desc
    </select>
    
    <insert id="insertStore" parameterType="VbStore" useGeneratedKeys="true" keyProperty="storeId">
        insert into vb_store (
            <if test="storeName != null and storeName != ''">store_name,</if>
            <if test="description != null and description != ''">description,</if>
            <if test="dimension != null">dimension,</if>
            <if test="vectorCount != null">vector_count,</if>
            <if test="status != null and status != ''">status,</if>
            <if test="remark != null and remark != ''">remark,</if>
            <if test="createBy != null and createBy != ''">create_by,</if>
            create_time
        )values(
            <if test="storeName != null and storeName != ''">#{storeName},</if>
            <if test="description != null and description != ''">#{description},</if>
            <if test="dimension != null">#{dimension},</if>
            <if test="vectorCount != null">#{vectorCount},</if>
            <if test="status != null and status != ''">#{status},</if>
            <if test="remark != null and remark != ''">#{remark},</if>
            <if test="createBy != null and createBy != ''">#{createBy},</if>
            sysdate()
        )
    </insert>
    
    <update id="updateStore" parameterType="VbStore">
        update vb_store 
        <set>
            <if test="storeName != null and storeName != ''">store_name = #{storeName},</if>
            <if test="description != null">description = #{description},</if>
            <if test="dimension != null">dimension = #{dimension},</if>
            <if test="vectorCount != null">vector_count = #{vectorCount},</if>
            <if test="status != null and status != ''">status = #{status},</if>
            <if test="updateBy != null and updateBy != ''">update_by = #{updateBy},</if>
            update_time = sysdate()
        </set>
        where store_id = #{storeId}
    </update>
    
    <update id="deleteStoreById" parameterType="Long">
        update vb_store set del_flag = '2' where store_id = #{storeId}
    </update>
    
    <update id="deleteStoreByIds" parameterType="String">
        update vb_store set del_flag = '2' where store_id in 
        <foreach item="storeId" collection="array" open="(" separator="," close=")">
            #{storeId}
        </foreach>
    </update>
    
</mapper>
